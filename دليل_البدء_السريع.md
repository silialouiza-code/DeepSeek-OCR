# 🚀 دليل البدء السريع - DeepSeek-OCR

## ما هو DeepSeek-OCR؟

**DeepSeek-OCR** هو نموذج ذكاء اصطناعي متقدم لاستخراج النصوص من الصور والمستندات وتحويلها إلى Markdown.

### المميزات الرئيسية:
- ✅ استخراج نصوص دقيق من الصور
- ✅ تحويل المستندات إلى Markdown
- ✅ التعرف على الجداول والمعادلات الرياضية
- ✅ معالجة ملفات PDF متعددة الصفحات
- ✅ دعم اللغة العربية

---

## 📦 التثبيت السريع

### الخطوة 1: إنشاء بيئة افتراضية

```bash
# إنشاء بيئة Python
conda create -n deepseek-ocr python=3.12.9 -y
conda activate deepseek-ocr
```

### الخطوة 2: تثبيت المكتبات

```bash
# تثبيت PyTorch
pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu118

# تثبيت المكتبات المطلوبة
pip install -r requirements.txt

# تثبيت Flash Attention (اختياري للأداء الأفضل)
pip install flash-attn==2.7.3 --no-build-isolation
```

### الخطوة 3: التحقق من التثبيت

```python
import torch
print(f"PyTorch: {torch.__version__}")
print(f"CUDA متاح: {torch.cuda.is_available()}")
print(f"عدد GPUs: {torch.cuda.device_count()}")
```

---

## 🎯 الاستخدام الأساسي

### مثال 1: استخراج نص من صورة

```python
from transformers import AutoModel, AutoTokenizer
import torch

# تحميل النموذج
model_name = 'deepseek-ai/DeepSeek-OCR'
tokenizer = AutoTokenizer.from_pretrained(model_name, trust_remote_code=True)
model = AutoModel.from_pretrained(model_name, trust_remote_code=True)
model = model.eval().cuda().to(torch.bfloat16)

# استخراج النص
prompt = "<image>\nFree OCR."
result = model.infer(
    tokenizer,
    prompt=prompt,
    image_file='your_image.jpg',
    output_path='./output',
    base_size=512,
    image_size=512,
    crop_mode=False
)

print(result)
```

### مثال 2: تحويل مستند إلى Markdown

```python
# تحويل مستند كامل مع الحفاظ على التنسيق
prompt = "<image>\n<|grounding|>Convert the document to markdown."
result = model.infer(
    tokenizer,
    prompt=prompt,
    image_file='document.jpg',
    output_path='./output',
    base_size=1024,
    image_size=640,
    crop_mode=True,
    save_results=True
)

print(result)
```

---

## 📝 الأوامر الشائعة (Prompts)

| الأمر | الاستخدام |
|-------|-----------|
| `<image>\nFree OCR.` | استخراج نص فقط |
| `<image>\n<|grounding|>Convert the document to markdown.` | تحويل إلى Markdown |
| `<image>\n<|grounding|>OCR this image.` | OCR عام |
| `<image>\nDescribe this image in detail.` | وصف الصورة |
| `<image>\nParse the figure.` | تحليل الأشكال |

---

## ⚙️ إعدادات الدقة

### اختيار الحجم المناسب:

```python
# Tiny: للصور الصغيرة (64 رمز بصري)
base_size=512, image_size=512, crop_mode=False

# Small: للصور المتوسطة (100 رمز بصري)
base_size=640, image_size=640, crop_mode=False

# Base: للصور القياسية (256 رمز بصري) - موصى به
base_size=1024, image_size=1024, crop_mode=False

# Large: للصور الكبيرة (400 رمز بصري)
base_size=1280, image_size=1280, crop_mode=False

# Gundam: للصور الديناميكية (أفضل أداء)
base_size=1024, image_size=640, crop_mode=True
```

---

## 🔧 حل المشاكل الشائعة

### مشكلة: نفاد ذاكرة GPU

```python
# الحل: استخدم حجم أصغر
base_size = 512  # بدلاً من 1024
image_size = 512
```

### مشكلة: نتائج غير دقيقة

```python
# الحل: استخدم حجم أكبر ووضع Grounding
prompt = "<image>\n<|grounding|>Convert the document to markdown."
base_size = 1280
image_size = 1280
```

### مشكلة: بطء المعالجة

```bash
# الحل: استخدم vLLM بدلاً من Transformers
cd DeepSeek-OCR-master/DeepSeek-OCR-vllm
python run_dpsk_ocr_image.py
```

---

## 📚 الأمثلة المتوفرة

تحقق من مجلد `examples/` للحصول على أمثلة مفصلة:

1. **example_1_basic_ocr.py** - استخراج نص بسيط
2. **example_2_document_to_markdown.py** - تحويل مستندات
3. **example_3_pdf_processing.py** - معالجة PDF

---

## 💡 نصائح سريعة

### للحصول على أفضل النتائج:

1. **جودة الصورة**: استخدم صوراً واضحة بدقة 300 DPI أو أكثر
2. **الإضاءة**: تأكد من إضاءة جيدة وبدون ظلال
3. **الحجم المناسب**: اختر base_size حسب حجم الصورة
4. **الأمر الصحيح**: استخدم `<|grounding|>` للمستندات المعقدة

### لتحسين الأداء:

1. **استخدم vLLM**: أسرع 5-10 مرات من Transformers
2. **المعالجة الدفعية**: عالج عدة صور دفعة واحدة
3. **GPU قوية**: يفضل A100 أو أفضل
4. **إدارة الذاكرة**: راقب استخدام ذاكرة GPU

---

## 🎓 التعلم المتقدم

للحصول على شرح مفصل، راجع:
- **شرح_DeepSeek_OCR_كامل.md** - شرح شامل مع أمثلة
- **examples/** - أمثلة عملية قابلة للتشغيل
- **README.md** - التوثيق الرسمي

---

## 📞 الدعم والمساعدة

- **GitHub**: https://github.com/deepseek-ai/DeepSeek-OCR
- **Hugging Face**: https://huggingface.co/deepseek-ai/DeepSeek-OCR
- **Discord**: https://discord.gg/Tc7c45Zzu5
- **الورقة البحثية**: https://arxiv.org/abs/2510.18234

---

## ✅ قائمة التحقق السريعة

قبل البدء، تأكد من:

- [ ] تثبيت Python 3.12 أو أحدث
- [ ] توفر GPU مع CUDA 11.8 أو أحدث
- [ ] ذاكرة GPU: 8GB على الأقل (16GB موصى به)
- [ ] تثبيت جميع المكتبات المطلوبة
- [ ] تحميل النموذج من Hugging Face

---

## 🚀 ابدأ الآن!

```bash
# انتقل إلى مجلد الأمثلة
cd examples/

# جرب المثال الأول
python example_1_basic_ocr.py

# أو جرب تحويل مستند
python example_2_document_to_markdown.py
```

---

**ملاحظة**: هذا دليل سريع للبدء. للحصول على شرح مفصل، راجع ملف `شرح_DeepSeek_OCR_كامل.md`.

**حظاً موفقاً! 🎉**
